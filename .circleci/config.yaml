version: 2
jobs:
  build:
    docker:
      - image: circleci/buildpack-deps:16.04-curl-dind
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      - run: mkdir build
      - run: cd build
      - run: cmake ..
      - run: make
      - run:
          name: Print the Current Time
          command: date
          
